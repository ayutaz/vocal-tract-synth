# PHASE5-001: LF声門モデル・声質制御・UI改善

**フェーズ**: Phase 5 — 品質向上
**マイルストーン**: [Phase 5](../MILESTONES.md#phase-5-声質向上ui改善--品質向上)
**状態**: 🔲 未着手
**前提条件**: Phase 4 完了（[PHASE4-001](PHASE4-001_auto-singing-mode.md)）
**成果物**: LF声門モデル + Rdパラメータ + 声質制御UI + UI全体リファイン

---

## 1. タスク目的とゴール

Phase 1-4で構築した声道シミュレーション音声合成アプリの最終仕上げフェーズ。声門音源をKLGLOTT88からLiljencrants-Fant（LF）モデルへ進化させ、Rdパラメータによる声質の連続的制御を実現する。併せてUI全体のリファインとパフォーマンス最適化を行い、プロダクション品質の「声の楽器」を完成させる。

### 達成目標

- LFモデルにより、KLGLOTT88では表現できなかった自然な声門波形を生成する
- Rdパラメータ1つで、チェストボイス（Rd=0.3）からファルセット（Rd=2.7）まで連続的に声質を遷移できる
- 気息ノイズ（Aspiration）の追加により、ブレシーボイスを表現可能にする
- UI全体を整えた完成形とし、操作性・視認性を最終水準に引き上げる
- 全フェーズの統合テストにより品質を保証する

---

## 2. 実装する内容の詳細

### 2.1 LF声門モデルの実装（Liljencrants-Fant モデル）

<!-- REVIEW: 技術的正確性レビューにて修正 — LFモデルが定義するのは声門流量の時間微分（dUg/dt）であることを明記。変数名をE(t)に修正し、文献との対応を明確化 -->
Liljencrants & Fant (1985) に基づく4パラメータ声門モデルを実装する。LFモデルは声門流量の時間微分 E(t) = dUg(t)/dt を開放相と閉鎖相の2区間で記述する。

**LF波形の数式定義:**

- 開放相 (0 <= t <= Te):
  ```
  E(t) = E0 * exp(alpha * t) * sin(omega_g * t)
  ```
  - omega_g = pi / Tp（声門開放の立ち上がり周期）
  - alpha: 振幅包絡を制御する指数パラメータ

- 閉鎖相 (Te <= t <= T0):
  ```
  E(t) = -(Ee / (epsilon * Ta)) * (exp(-epsilon * (t - Te)) - exp(-epsilon * (T0 - Te)))
  ```
  - Ee: 最大励振振幅（Te時点での流量微分のピーク値、E(Te) = -Ee）
  - Ta: リターンフェーズの時定数（声門閉鎖の急峻さ）
  - epsilon: 1/Ta から暗黙方程式で求まる減衰係数

**暗黙方程式の数値解法:**

alpha の決定には以下の暗黙方程式を解く必要がある:

```
(1/alpha) - (1/(alpha + (omega_g^2 / alpha))) = Te - Tp
```

Newton-Raphson法による反復求解を実装する:
- 初期推定値: alpha_0 = 1 / Tp
- 収束判定: |f(alpha)| < 1e-10（10回以内に収束）
- 発散防止: alpha の上下限クランプ

epsilon の決定:
```
1 - exp(-epsilon * (T0 - Te)) = epsilon * Ta
```
同様にNewton-Raphson法で解く。

**実装場所**: `src/models/glottal-source.ts` に `LFGlottalSource` クラスを追加

**波形生成の実装方針:**
- 位相アキュムレータ方式は既存KLGLOTT88と同様（`phase += F0 / sampleRate`）
- 1周期ごとにRdパラメータからLFパラメータを再計算
- process()内ではルックアップテーブル方式も検討（計算コスト軽減）

### 2.2 Rdパラメータによる声質制御

Fant (1995) の簡易パラメータ化に基づき、単一のRdパラメータから全LFパラメータを導出する。

**Rdの意味と範囲:**

| Rd 値 | 声質 | 特徴 |
|-------|------|------|
| 0.3 - 0.7 | 押し声（Pressed） | 声門閉鎖が急峻、倍音豊富 |
| 0.7 - 1.2 | チェストボイス（Modal） | 通常の発声、バランスの取れた倍音構造 |
| 1.2 - 2.0 | 柔らかい声（Lax） | 緩やかな閉鎖、高域倍音が少ない |
| 2.0 - 2.7 | ファルセット（Breathy） | 声門閉鎖が不完全、気息成分多い |

<!-- REVIEW: 技術的正確性レビューにて修正 — Fant (1995) のRd→LFパラメータ変換式を修正。Rapのコメント(Tp/Te比→Ta/T0比)、Tp/Te/Taの導出手順を正しい定義に修正 -->
**Rd → LFパラメータ変換式:**

```typescript
// Rd から正規化パラメータへの変換（Fant 1995）
const Rap = (-1 + 4.8 * Rd) / 100;                    // Ra = Ta/T0 比
const Rkp = (22.4 + 11.8 * Rd) / 100;                 // Rk = (Te-Tp)/Tp 比
const Rgp = 1 / (4 * ((0.11 * Rd / (0.5 + 1.2 * Rkp)) - Rap)); // Rg = T0/(2*Tp)

// 正規化パラメータ → 絶対パラメータ（T0 = 1/F0 に基づく）
const T0 = 1.0 / F0;
const Tp = T0 / (2 * Rgp);                             // 声門開放ピーク時刻
const Te = Tp * (1 + Rkp);                             // 最大励振時刻
const Ta = Rap * T0;                                    // リターンフェーズ時定数
const Ee = -1.0;                                        // 正規化（振幅はF0と連動で調整）
```

注: 上記は簡略化した変換。実装時にはFant (1995) およびDegottex et al. (2010) の厳密な変換式を参照し、数値安定性を検証すること。特にRdが極端な値（0.3付近や2.7付近）ではRgpが不安定になりうるため、Tp/Te/Taの物理的妥当性（0 < Tp < Te < T0, Ta > 0）を必ず検証すること。

**実装場所**: `src/models/glottal-source.ts` 内に `rdToLFParams()` 関数を追加

### 2.3 KLGLOTT88 → LFモデルの切替

Phase 2で実装済みのKLGLOTT88と、新規実装のLFモデルを選択可能にする。

**実装方針:**
- `GlottalSourceInterface` を定義（共通のメソッドシグネチャ）
- `KLGLOTT88Source` と `LFSource` の2実装を用意
- postMessage でモデル切替コマンドを送信
- 切替時: 現在の声門周期が完了してから次の周期でモデルを切り替える（不連続性防止）
- デフォルトはLFモデル（Phase 5完了後）

**AudioWorklet内での切替:**
```typescript
// worklet-processor.ts 内
interface GlottalModel {
  generate(phase: number): number;
  setParams(params: GlottalParams): void;
}

// モデル切替はゼロクロス付近で行う（phase ≈ 0のタイミング）
```

### 2.4 気息ノイズ（Aspiration）の追加

声門閉鎖が不完全な場合に生じるノイズ成分を追加する。

**実装内容:**
- 白色ノイズをバンドパスフィルタ（200Hz-3000Hz）で帯域制限
- Aspirationパラメータ（0.0-1.0）で混合比を制御
- RdパラメータとAspiration量の連動オプション（Rd > 1.5 で自動的に気息が増加）
- 声門周期の閉鎖相（Te-T0区間）にのみノイズを注入する方式も検討

**混合式:**
```typescript
output = (1 - aspirationLevel) * lfWaveform + aspirationLevel * filteredNoise;
```

**実装場所**: `src/models/glottal-source.ts` 内の音源生成ループに統合

### 2.5 声質制御UIスライダー

**UI要素:**
- Rd スライダー: 範囲 0.3 - 2.7、ステップ 0.1、デフォルト 1.0（Modal voice）
- Aspiration スライダー: 範囲 0.0 - 1.0、ステップ 0.01、デフォルト 0.0
- 声門モデル切替: ドロップダウンまたはトグル（KLGLOTT88 / LF）
- 各スライダーに現在値の数値表示
- Rdスライダーの下に声質ラベル表示（Pressed / Modal / Lax / Breathy）

**配置**: コントロールパネル内、F0スライダーの近傍に配置

<!-- REVIEW: アーキテクチャレビューにて修正 — Rd の通信方式と Phase 4 ビブラートとの整合性を明記。 -->
**通信**: Rdパラメータは postMessage で AudioWorklet に送信（`{ type: 'setRd', rd: number }` — Phase 1 の通信プロトコル型定義に追加済み）。AudioParam化は将来の MIDI/OSC 対応時に検討する。

**Phase 4 ビブラートとの整合**: ビブラートは F0 の AudioParam を `setValueAtTime` / `linearRampToValueAtTime` で操作する。Rd は postMessage のため、ビブラート中に Rd を連動変動させる将来拡張（Phase 5 セクション 7.1 参照）では、postMessage の送信頻度が増加する。ただしビブラート連動 Rd は 5.5Hz = 約 180ms 周期で 1 回の postMessage 送信であり、メインスレッドの負荷は無視できる。Rd を AudioParam 化する判断は、MIDI CC による外部制御の需要が具体化した時点で行う。

### 2.6 UI全体のリファイン

Phase 1-4で逐次追加したUI要素を統一的に整理する。

**レイアウト調整:**
- コントロールパネルの論理的グルーピング（音源パラメータ / 声道パラメータ / 表示設定 / 自動モード）
- 各セクションに明確なラベルとセパレータ
- ボタン・スライダーの一貫したサイズとスペーシング
- 全体の視覚的階層構造の確立

**レスポンシブ対応の考慮:**
- Canvas要素のリサイズ対応確認（ResizeObserver、Phase 1で実装済み）
- コントロールパネルの折り返し対応（flex-wrap）
- 最小幅の設定（800px程度以下では一部UIを折りたたむ等）
- タッチデバイスでの操作性確認（Phase 1の Pointer Events 実装の検証）

**視覚的改善:**
- 声道エディタ Canvas の配色見直し（背景色、制御点の色、補間曲線の色）
- スペクトル表示の配色調整（フォルマントマーカーの視認性向上）
- 状態表示の明確化（Start/Stop, Auto ON/OFF の状態が一目でわかるデザイン）
- CSSカスタムプロパティによるテーマ管理

<!-- REVIEW: アーキテクチャレビューにて修正 — worklet-processor.ts の肥大化リスクへの対処を追記。 -->
### 2.7a worklet-processor.ts のモジュール分解

Phase 5 完了時点で worklet-processor.ts が担う責務が多く（2種の声門モデル + KLフィルタ + 放射フィルタ + 壁面損失 + 有声/無声混合 + 気息ノイズ + ジッター/シマー）、1ファイルでの管理が困難になる。

**分解方針**: worklet-processor.ts は各モジュールを呼び出すオーケストレーターに徹し、計算ロジックは別モジュールに分離する。AudioWorklet ファイルは `new URL()` パターンで単一エントリとしてビルドされるが、Vite はワーカーファイル内の import を解決するため、モジュール分割が可能。

```
src/audio/
  worklet-processor.ts          # オーケストレーター: process() のメインループ
src/models/
  glottal-source.ts             # GlottalSourceInterface + KLGLOTT88Source + LFSource
  vocal-tract.ts                # KLアルゴリズム（散乱計算 + 壁面損失）
  radiation-filter.ts           # 放射フィルタ（1次IIR）+ 唇端反射  ← Phase 5 で分離
  output-mixer.ts               # 有声/無声/気息ノイズの混合       ← Phase 5 で分離
```

**process() メインループの構造**:
```typescript
process(inputs, outputs, parameters) {
  const output = outputs[0][0];
  const f0 = parameters.frequency[0];

  for (let i = 0; i < 128; i++) {
    // 1. 声門音源生成（KLGLOTT88 or LF + 気息ノイズ混合）
    const glottalSample = this.glottalSource.generate(this.phase, f0, this.jitter, this.shimmer);
    // 2. KLフィルタ（散乱計算 + 壁面損失）
    this.vocalTract.processSample(glottalSample);
    // 3. 放射フィルタ
    output[i] = this.radiationFilter.process(this.vocalTract.getLipOutput());
    // 4. 位相アキュムレータ更新
    this.phase += f0 / sampleRate;
    if (this.phase >= 1.0) this.phase -= 1.0;
  }
  return true;
}
```

各モジュールはクラスのメソッド呼び出しであり、process() 内で new や配列生成は発生しない。全バッファは各クラスのコンストラクタで事前確保する。

### 2.7b パフォーマンス最適化の最終調整

全Phase統合後の最終パフォーマンスチューニング。

**AudioWorklet スレッド:**
- LFモデル追加後のCPU負荷計測（performance.now() による process() 実行時間の監視）
- LFパラメータのルックアップテーブル化（1周期分の波形を事前計算、F0またはRd変更時に再生成）
- process() 内のメモリアロケーション完全排除の再確認
- 型付き配列（Float64Array）の適切な使用

**メインスレッド:**
- requestAnimationFrame のフレームドロップ監視
- Canvas 描画の最適化（不変部分のキャッシュ、dirty rect 管理）
- フォルマント計算（QR法）の実行頻度調整（断面積変更時のみ再計算）
- postMessage の送信頻度制限（throttle: 最大60回/秒）

<!-- REVIEW: アーキテクチャレビューにて修正 — Phase間累積のprocess()計算量見積もりを追記。 -->
**Phase間累積 process() 計算量見積もり** (128サンプルあたり):

| Phase | 追加処理 | 1サンプルあたりの推定演算 | 128サンプルの推定時間 |
|-------|---------|------------------------|---------------------|
| 1 | パルス音源 + KL(44区間散乱) + 差分放射 | ~200 ops | ~0.3ms |
| 2 | + KLGLOTT88(多項式4-5ops) + 壁面損失(88乗算) | ~300 ops | ~0.5ms |
| 3 | (Worklet側追加なし) | ~300 ops | ~0.5ms |
| 4 | + ジッター/シマー(乱数+乗算) | ~310 ops | ~0.5ms |
| 5a | + LF(exp+sin/サンプル) | ~350 ops | ~0.6ms |
| 5b | + LF(LUT方式、線形補間) | ~310 ops | ~0.5ms |
| 5c | + 気息ノイズ(BPF 2次IIR) | +20 ops | +0.03ms |
| 5d | + Newton-Raphson(1周期に1回) | ~50 ops/周期 | ~0.01ms(償却) |

**合計見積もり (Phase 5 最終)**:
- LF直接計算方式: ~0.65ms / 128samples (予算 2.9ms の 22%)
- LUT方式: ~0.55ms / 128samples (予算 2.9ms の 19%)

いずれもレンダリング予算の 50% 目標を十分に満たす。LUT 方式は計算コスト削減効果が限定的だが、Newton-Raphson の収束問題を回避できる利点がある。

**メインスレッド累積計算量見積もり** (1フレーム = 16.7msあたり):

| 処理 | Phase | 推定時間 | 頻度 |
|------|-------|---------|------|
| Canvas声道描画 | 1 | ~1ms | 60fps |
| スプライン補間(16→44) | 1 | ~0.1ms | ドラッグ時/Auto時 |
| Canvasスペクトル描画 | 3 | ~2ms | 60fps |
| QR法フォルマント計算 | 3 | 1-3ms | 10-15fps (throttle) |
| Auto Singロジック | 4 | ~0.5ms | 25ms間隔(setInterval) |
| 母音補間 + postMessage | 4 | ~0.2ms | 60fps(Auto時) |

最悪ケース合計 (Auto Sing中、QR法再計算フレーム): ~7ms / 16.7ms (42%)。60fps維持に十分な余裕がある。

**計測基準:**
- process() 実行時間: 128 samples のレンダリング予算（2.9ms）の50%以下が目標
- メインスレッド: 60fps 維持、フレームドロップ率 1% 以下
- メモリ使用量: 安定状態で増加しないこと（GCリークなし）

### 2.8 全フェーズ統合テスト

Phase 1-5 の全機能を統合した状態での品質保証。

**統合テスト項目:**
- 全母音プリセット (a/i/u/e/o) × 全声質 (Pressed/Modal/Lax/Breathy) の組み合わせ
- Autoモード + LFモデル + Rdスライダー操作の同時動作
- 長時間動作安定性（30分以上の連続再生でクラッシュ・メモリリークがないこと）
- ブラウザ間テスト（Chrome, Firefox, Edge）
- パラメータの極端値テスト（Rd最小/最大、F0最低/最高、断面積最小/最大の同時設定）

---

## 3. 実装に必要なエージェントチームの役割と人数

| エージェント名 | 役割 | 主な担当範囲 | 想定工数 |
|---------------|------|-------------|---------|
| **lf-model** | LF声門モデル数値実装 | LF波形生成、暗黙方程式の Newton-Raphson 解法、Rd→(Tp, Te, Ta, Ee) パラメータ変換、alpha/epsilon の数値安定な求解、位相アキュムレータとの統合 | 中-大 |
| **voice-quality** | 声質制御ロジック | 気息ノイズ生成とバンドパスフィルタリング、Aspiration混合ロジック、Rd連動の自動Aspiration、KLGLOTT88/LF切替のゼロクロスタイミング制御、GlottalSourceInterface の設計 | 中 |
| **ui-refinement** | UI全体のレイアウト調整 | コントロールパネル再構成、Rd/Aspirationスライダー追加、声質ラベル表示、CSS整理とテーマ管理、レスポンシブ対応、視覚的改善 | 中 |
| **performance** | 全体パフォーマンス最適化 | LFモデルのルックアップテーブル化、process()のプロファイリング、Canvas描画最適化、メモリリーク調査、パフォーマンスベースライン記録 | 小-中 |
| **qa-integration** | 全フェーズ統合テスト | Phase 1-5の全機能統合テスト、ブラウザ間互換テスト、長時間安定性テスト、極端値テスト、最終品質承認 | 中 |

### エージェント間の依存関係

```
lf-model ──────────┐
                    ├──→ performance ──→ qa-integration
voice-quality ─────┘                        ↑
                                            │
ui-refinement ──────────────────────────────┘
```

- `lf-model` と `voice-quality` は並行作業可能（インターフェース定義を先に合意）
- `ui-refinement` は他と独立して作業可能（postMessage のパラメータ追加のみ依存）
- `performance` は lf-model と voice-quality の完了後に着手
- `qa-integration` は全エージェントの成果物統合後に実施

---

## 4. 提供範囲とテスト項目

### 4.1 スコープ内

- LF声門モデルの完全実装（波形生成 + パラメータ変換 + 暗黙方程式の数値解法）
- Rdパラメータ（0.3-2.7）による声質の連続制御
- 気息ノイズの生成と混合
- KLGLOTT88 / LF モデルの切替機能
- 声質制御UI（Rdスライダー、Aspirationスライダー、モデル切替）
- UI全体のレイアウト整理・視覚的改善
- パフォーマンス最適化の最終調整
- 全フェーズ統合テスト

### 4.2 スコープ外

- テキストから音素列への変換（TTS機能）— 将来の拡張
- MIDI / OSC 外部入力対応 — 将来の拡張
- 鼻音・摩擦音の声道モデル拡張（鼻腔分岐管、乱流ノイズ源）
- 多言語対応の母音プリセット
- モバイル専用UI / PWA対応
- ユーザーによるプリセット保存機能
- 録音・書き出し機能（WAVエクスポート等）
- 声道形状の3Dビジュアライゼーション

### 4.3 ユニットテスト

<!-- REVIEW: 技術的正確性レビューにて修正 — 変数名をE(t)に統一 -->
<!-- REVIEW: テスト戦略レビューにて修正 — Rd境界値テスト・F0×Rd組み合わせテスト・パフォーマンス比較テスト・モデル切替テスト・長時間安定性テスト追加、成功基準トレーサビリティを付与 -->
#### LF波形の連続性テスト
```
テスト名: LF波形（流量微分 E(t)）が周期境界で連続であること
条件: Rd = 0.3, 1.0, 1.5, 2.0, 2.7 の各値で1周期生成
検証:
  - E(0) ≈ 0（周期開始時の流量微分がゼロ付近）
  - E(T0) ≈ 0（周期終了時の流量微分がゼロ付近）
  - |E(T0) - E(0)| < epsilon（周期境界の不連続量が十分小さい）
  - 波形にNaN/Infinityが含まれないこと
```

#### Rdパラメータ → Tp/Te/Ta 変換の正確性テスト
```
テスト名: Rd→LFパラメータ変換がFant (1995) の参照値と一致すること
条件: Rd = 0.3, 0.5, 1.0, 1.5, 2.0, 2.7
検証:
  - Tp/T0 比が Rd の増加に対して単調増加
  - Te/T0 比が Rd の増加に対して単調増加
  - Ta/T0 比が Rd の増加に対して単調増加
  - 各パラメータが物理的に妥当な範囲内（0 < Tp < Te < T0, Ta > 0）
  - Rd = 1.0 でのパラメータ値が文献の参照値と±5%以内で一致
```

#### 気息ノイズの混合比テスト
```
テスト名: Aspirationパラメータが混合比を正しく制御すること
条件: aspiration = 0.0, 0.5, 1.0
検証:
  - aspiration = 0.0: 出力がLF波形のみ（ノイズ成分なし）
  - aspiration = 1.0: 出力がフィルタ済みノイズのみ
  - aspiration = 0.5: 出力のRMS値が LF のみ / ノイズのみ の中間
  - ノイズのスペクトルがバンドパスフィルタ（200Hz-3000Hz）の特性を持つこと
```

#### Newton-Raphson 収束テスト
```
テスト名: alpha/epsilon の Newton-Raphson 求解が全Rd範囲で収束すること
条件: Rd = 0.3 から 2.7 まで 0.01 刻み、F0 = 80Hz から 400Hz まで
検証:
  - 全ケースで10回以内に収束
  - 収束後の残差 < 1e-10
  - 結果の alpha > 0, epsilon > 0
```

#### LF波形 — Rd境界値テスト
```
テスト名: Rdの極端値でLF波形が破綻しないこと
条件: Rd = 0.3（最小値, Pressed）, Rd = 2.7（最大値, Breathy）
検証:
  - 波形にNaN/Infinityが含まれないこと
  - 波形値の絶対値が合理的な範囲内（|E(t)| < 10）であること
  - 周期開始点と終了点が連続であること（|E(T0) - E(0)| < 0.01）
  - Rd=0.3のスペクトル傾斜がRd=2.7より緩やかであること（高域倍音が多い）
```

#### LF波形 — F0 x Rd 組み合わせ安定性テスト
```
テスト名: F0とRdの全範囲の組み合わせで数値的に安定であること
条件: F0 = {50, 120, 200, 400}Hz × Rd = {0.3, 1.0, 2.0, 2.7}（16パターン）
検証:
  - 全16パターンで1000サンプル生成してNaN/Infinityがないこと
  - Newton-Raphsonが全パターンで収束すること
  - Tp < Te < T0 の不等式が全パターンで成立すること
```

#### KLGLOTT88 ↔ LFモデル切替テスト
```
テスト名: モデル切替時に波形が不連続にならないこと
条件: F0=120Hz, 声門位相=0付近（ゼロクロス）で切替を実行
検証:
  - 切替前後の波形の隣接サンプル差分が閾値（0.1）以下であること
  - 切替後100ms以内に新モデルの波形が安定すること
  - 高速な切替連打（10回/秒）でNaN/クラッシュが発生しないこと
```

#### パフォーマンステスト — LFモデル vs KLGLOTT88
```
テスト名: LFモデルの計算コストがKLGLOTT88と比較して許容範囲内であること
検証方法: 同一条件（F0=120Hz, 44区間均一管）で128サンプルのprocess()を100回計測
基準値:
  - LFモデル（テーブル不使用）: KLGLOTT88の3倍以内
  - LFモデル（テーブル使用）: KLGLOTT88の1.5倍以内
  - いずれの場合もprocess()実行時間がレンダリング予算（2.9ms）の50%以下
成功基準カバレッジ: → REQUIREMENTS.md 8項「低レイテンシに動作する」
```

#### 長時間数値安定性テスト
```
テスト名: 30分相当のLFモデル連続動作で発散しないこと
条件: F0=120Hz, Rd=1.0, 均一管, 30分 = 44100×1800 = 79,380,000サンプル
検証:
  - 最後の1秒のRMS振幅が最初の1秒の10倍を超えないこと
  - NaN/Infinityが含まれないこと
  - 位相アキュムレータのドリフトが1e-6以下であること
```

### 4.4 E2Eテスト

<!-- REVIEW: テスト戦略レビューにて修正 — 自動化方針の具体化、極端値テスト追加、全Phase統合リグレッションチェック追加、成功基準トレーサビリティを付与 -->

#### テスト自動化方針
- **Rdスライダーの自動操作**: Playwright で `page.evaluate()` 内から range input の値を段階的に変更し、各段階のスペクトルデータを取得して高域倍音の減衰傾向を数値検証
- **20パターンの自動テスト**: 5母音 x 4 Rd値のマトリクスを自動実行し、各パターンで RMS振幅 > 0.01（音が出ている）かつ NaN/Infinity なしを検証
- **長時間安定性テスト**: Playwright の test timeout を 5分に設定し、3分間の連続動作後にメモリ使用量を `page.evaluate(() => performance.memory)` で取得（Chrome限定）

#### Rdスライダー → 声質変化確認
```
テスト名: RdスライダーでリアルタイムにLF波形が変化すること
手順（Playwright自動化可能 — スペクトルデータベース検証）:
  1. アプリを起動し Start を押す
  2. 声門モデルを LF に設定
  3. Rd スライダーを 0.3 に設定 → スペクトルデータ取得 → 高域エネルギー算出
  4. Rd スライダーを 2.7 に設定 → スペクトルデータ取得 → 高域エネルギー算出
検証:
  - 音が途切れないこと（RMS振幅 > 0.01が継続）
  - Rd=0.3 の高域エネルギー > Rd=2.7 の高域エネルギー（倍音構造の変化が数値的に確認できる）
  - 操作から音変化までのレイテンシが知覚できない程度であること
```

#### 全母音 x 全声質の組み合わせテスト
```
テスト名: 5母音 x 4声質レンジの全組み合わせが安定動作すること
手順（Playwright自動化可能 — 20パターンの自動実行）:
  1. 声門モデルを LF に設定
  2. Rd を 0.5（Pressed）に設定 → 各母音プリセット（a/i/u/e/o）のボタンをクリック → 各300ms待機 → RMS/NaN検証
  3. Rd を 1.0（Modal）に設定 → 同上
  4. Rd を 1.5（Lax）に設定 → 同上
  5. Rd を 2.5（Breathy）に設定 → 同上
検証:
  - 全20パターンで音が出力されること（RMS振幅 > 0.01）
  - 全パターンでフォルマント表示が妥当な値を示すこと（F1/F2の母音依存パターンが維持される）
  - 音量の極端な変動がないこと
  - NaN/Infinity によるノイズバーストが発生しないこと
```

#### Autoモード + LFモデルの動作テスト
```
テスト名: 自動歌唱モードとLFモデルが同時に正常動作すること
手順:
  1. 声門モデルを LF に設定
  2. Rd を 1.0 に設定
  3. Auto モードを ON にする
  4. 自動歌唱中に Rd スライダーを操作する
  5. 3分以上連続動作させる
検証:
  - 自動母音遷移とLF波形生成が競合しないこと
  - Rdの動的変更が反映されること
  - フレーズ間の休符でノイズが出ないこと
  - 3分後にメモリ使用量が安定していること（増加傾向がないこと）
```

#### 極端値同時設定テスト
```
テスト名: パラメータ極端値の同時設定で安定動作すること
手順:
  1. 声門モデルを LF に設定
  2. Rd = 0.3（最小）, F0 = 400Hz（最高）, 全断面積 = 0.3cm²（最小）を同時設定
  3. Rd = 2.7（最大）, F0 = 50Hz（最低）, 全断面積 = 10.0cm²（最大）を同時設定
  4. Aspiration = 1.0（最大）+ LFモデル + Auto Sing ONを同時設定
検証:
  - 全パターンでクラッシュ/NaN/Infinityが発生しないこと
  - process()がブロックされないこと（音が途切れない）
```

#### Phase 1-4 全機能統合リグレッションチェック
```
テスト名: Phase 5 の全変更が Phase 1-4 の機能を破壊していないこと
検証:
  - Phase 1 全ユニットテストのパス（KLアルゴリズム、反射係数、スプライン補間）
  - Phase 2 全ユニットテストのパス（KLGLOTT88波形、壁面損失、プリセット）
  - Phase 3 全ユニットテストのパス（フォルマント計算、step-up/down、QR法）
  - Phase 4 全ユニットテストのパス（マルコフ連鎖、ビブラート、リズム、フレーズ）
  - 声門モデルをKLGLOTT88に設定した場合、Phase 2-4 の動作がPhase 4完了時と同等であること
  - CIパイプラインで全Phaseのユニットテストを実行し、全パスをゲート条件とすること
```
- **成功基準カバレッジ**: Phase 5 E2Eテストは REQUIREMENTS.md 8項の全5項目を網羅的にカバーする最終検証

#### REQUIREMENTS.md 成功基準との最終マッピング
```
成功基準1「声道断面積をドラッグで変更でき、リアルタイムに音が変化する」
  → Phase 1 E2E (自動: ドラッグ→スペクトル変化検証) + Phase 5 統合リグレッション

成功基準2「5母音のプリセットが正しい母音に聞こえる」
  → Phase 2 E2E (自動: FFTフォルマント検証) + Phase 3 ユニット (フォルマント照合)

成功基準3「スペクトル表示でフォルマント構造の違いが視認できる」
  → Phase 3 E2E (自動: DOM数値検証 + スペクトルデータ検証)

成功基準4「自動モードで『歌っている』ように聞こえる」
  → Phase 4 E2E (自動: F0時系列解析) + Phase 4 手動 (聴覚評価)

成功基準5「低レイテンシに動作する」
  → Phase 1-5 パフォーマンステスト (process()時間、フレームレート、QR法時間)
  → Phase 5 パフォーマンスベースライン記録表
```

---

## 5. 実装に関する懸念事項とレビュー項目

### 5.1 LFモデルの暗黙方程式の数値解法

**懸念**: alpha と epsilon の決定に Newton-Raphson 法が必要であり、AudioWorklet の process() 内で反復計算を行うとリアルタイム性を損なう可能性がある。

**対策:**
- Newton-Raphson は1周期に1回のみ実行（毎サンプルではない）。F0=400Hz でも 44100/400=110 サンプルに1回。
- 初期推定値を前回の収束値から開始（warm start）することで反復回数を削減
- 最悪ケースでも10回以内に収束するよう上下限クランプを設置
- 代替案: Rd の離散グリッドで事前計算したテーブルを補間（完全に反復を排除）

**レビュー項目:**
- [ ] Newton-Raphson の収束条件と最大反復回数の妥当性
- [ ] 発散時のフォールバック処理（前回の値を使用）
- [ ] process() 内での実行時間計測結果

### 5.2 Rdパラメータ範囲外での安定性

**懸念**: Rd が極端な値（0.3未満や2.7超）に設定された場合、LFパラメータ変換式が物理的に意味のない値を返す可能性がある。

**対策:**
- UI側でスライダーの範囲を 0.3-2.7 にハードリミット
- 計算側でもクランプ: `Rd = Math.max(0.3, Math.min(2.7, Rd))`
- Tp, Te, Ta の各パラメータに独立した妥当性検証（0 < Tp < Te < T0, Ta > 0）
- 不正値検出時はデフォルト値（Rd=1.0相当）にフォールバック

**レビュー項目:**
- [ ] Rdの全範囲（0.3-2.7、0.01刻み）でのパラメータ変換結果の検証
- [ ] F0の全範囲（80-400Hz）との組み合わせでの安定性
- [ ] フォールバック処理のテストカバレッジ

### 5.3 KLGLOTT88との切替時の不連続性

**懸念**: モデル切替時に波形の不連続が発生し、クリック音やポップノイズが聞こえる可能性がある。

**対策:**
- 声門周期の開始点（phase ≈ 0）でのみモデルを切り替える
- 切替時に5-10msのクロスフェードを適用（2モデルを一時的に並行実行）
- 切替指示はフラグとして保持し、次のゼロクロスで実行

**レビュー項目:**
- [ ] 切替時のオーディオ波形のオシロスコープ検査（不連続の有無）
- [ ] 高速な切替連打での安定性
- [ ] 切替中のパラメータ状態の一貫性

### 5.4 全体のCPU負荷

**懸念**: LFモデルはKLGLOTT88より計算コストが高い（指数関数 exp() と三角関数 sin() を毎サンプル計算）。44区間KLフィルタ + LF音源 + 気息ノイズフィルタの合計負荷が process() の予算（2.9ms）を超える可能性がある。

**対策:**
- LF波形のルックアップテーブル化: 1周期分を1024点で事前計算、線形補間で読み出し
  - テーブル再計算: F0またはRdが変化した周期のみ（変化がなければテーブル再利用）
  - テーブルサイズ: Float64Array(1024) = 8KB（十分小さい）
- Math.exp() / Math.sin() の高速近似関数の検討（精度とのトレードオフ）
- 気息ノイズのバンドパスフィルタを2次IIR（biquad）に限定（計算コスト最小）

**レビュー項目:**
- [ ] process() の実行時間計測（LFモデル有効時 vs KLGLOTT88時の比較）
- [ ] ルックアップテーブル方式の音質劣化の有無（テーブルサイズの妥当性）
- [ ] 負荷ピーク時（F0最高 + Rd変更 + 断面積変更が同時発生）の挙動

### 5.5 その他のレビュー項目

- [ ] LF波形の物理的妥当性（文献値との比較、声門流量の時間積分がゼロに近いこと）
- [ ] 気息ノイズのバンドパスフィルタ係数の正当性
- [ ] UI追加によるメインスレッドの負荷増分
- [ ] postMessage の送信頻度（Rdスライダーのドラッグ中）
- [ ] 全フェーズ統合後のメモリプロファイル

---

## 6. 一から作り直すとしたら

### 6.1 Phase 1-5 全体を統合した設計観点

Phase 1-5 を最初から見通して設計するなら、以下の点を変更する。

#### 最初からLFモデルベースで始めるべきだったか

**結論: 段階的にKLGLOTT88 → LFが正解だった。**

理由:
- KLGLOTT88 は多項式ベースで暗黙方程式が不要。Phase 1-2 で「音が出る」状態を早期に達成できた
- LFモデルの Newton-Raphson 解法のデバッグは、声道フィルタ（KL + 壁面損失 + 放射フィルタ）が正しく動作している状態で行うほうが、音源の問題か声道の問題かの切り分けが容易。Phase 2 で壁面損失が導入済みであることが前提条件
- KLGLOTT88 がベースラインとして存在することで、LFモデルの音質をA/B比較できる
- 段階的実装により、各フェーズで動作する成果物を確認しながら進められた

この設計判断は、Phase 2 の rebuild で述べた「壁面損失を Phase 1 から入れるべきだった」という提案と整合する。壁面損失は Phase 1 から入れ、声門音源は段階的に高品質化する、というのが全体を通じた最適戦略であった

ただし改善点として:
- Phase 1 の時点で `GlottalSourceInterface` を定義しておけば、Phase 5 での切替実装がよりクリーンだった。Phase 1 の rebuild で「過度な抽象化は不要」としつつ「関数シグネチャの統一」を推奨したが、この判断は正しかった。Phase 2 で `GlottalSource` インターフェースを抽出するタイミングが最適であり、Phase 1 時点でのインターフェース定義は YAGNI（必要になるまで作らない）の範囲で十分だった
- Rdパラメータの概念をPhase 2のKLGLOTT88実装時にOQ（Open Quotient）として先取りしておけば、UIの連続性が向上した

#### AudioWorklet プロセッサの全体アーキテクチャ

最初から設計するなら:
- **コマンドパターンの導入**: postMessage で送るコマンドを型安全な判別共用体型（discriminated union）で管理。Phase が増えるごとに `if/else` が増殖するのを防ぐ。ただし AudioWorklet 内のコマンドディスパッチは、GC 回避のため `switch` 文 + プリミティブ型比較に限定し、Map/オブジェクト生成を伴うパターンは避ける
- **モジュール分離**: worklet-processor.ts 内の責務を明確に分離
  ```
  worklet-processor.ts
    ├── GlottalSource (インターフェース + 実装2種)
    ├── VocalTractFilter (KL アルゴリズム)
    ├── RadiationFilter
    └── OutputMixer (有声/無声/気息の混合)
  ```
- **状態管理の一元化**: パラメータの二重管理（UI側とWorklet側）を防ぐため、パラメータストアの設計を最初に行う
- **ダブルバッファリング**: 断面積配列の更新をダブルバッファ方式にし、process() のフレーム途中で断面積が切り替わる不整合を防ぐ。なお postMessage は構造化クローンでデータをコピーするため、メインスレッドとの競合は発生しない。ダブルバッファの目的はコピー回避ではなく、`port.onmessage` による受信タイミングと `process()` の実行タイミングの不整合を防ぐ点にある（受信は process() の外で発生し、process() 実行中に断面積が変わらないことを保証する）

#### UIコンポーネントの設計パターン

最初から設計するなら:
- **イベントバスの導入**: UI要素間の通信を publish/subscribe パターンで疎結合化。Phase 4 の rebuild で提案した `SynthEvent` / `EventScheduler` 型のイベントシステム（手動/自動/MIDI の統一インターフェース）と同一の設計思想であり、Phase 1 の時点でこの基盤を敷いておけば Phase 4 の統合コーディネータが大幅に簡素化できた
- **UIステートマシン**: アプリの状態（停止中 / 再生中 / Auto中 / パラメータ編集中）を明示的なステートマシンで管理
- **CSSカスタムプロパティの体系化**: 配色・スペーシング・フォントサイズをCSS変数で一元管理し、Phase ごとのUI追加で統一感を維持
- **Canvas描画の抽象化層**: 声道エディタとスペクトル表示で共通の描画ユーティリティを先に設計

#### テスト戦略の全体最適化

最初から設計するなら:
- **Phase 1でテストインフラを構築**: Vitest の導入と AudioWorklet のモック環境の整備。AudioWorklet の process() 内のロジック（KLアルゴリズム、音源生成）は純粋な数値計算モジュールとして分離してあるため（Phase 1 rebuild 6.2 参照）、Node.js 上でのユニットテストが可能。Canvas のスナップショットテストは費用対効果が低いため優先度は下げてよい
- **各PhaseにCI/CDゲートを設置**: Phase N の完了条件をCIで自動検証してから Phase N+1 に進む。具体的には、KL均一管の共鳴周波数テスト（Phase 1）、KLGLOTT88波形連続性テスト（Phase 2）、フォルマント計算精度テスト（Phase 3）等をCIに組み込む
- **音声品質の自動テスト**: 合成音声のスペクトル特性を自動検証するテストフレームワーク。ただし実現にはオフラインレンダリング（OfflineAudioContext）を使ったテスト用パイプラインが必要で、AudioWorklet の OfflineAudioContext 対応はブラウザ依存の制約がある。現実的には、数値計算モジュール単体でインパルス応答を生成し、FFT でフォルマントピークを検出する方式が堅実
- **パフォーマンスリグレッションテスト**: process() の実行時間をCIで計測し、閾値超過でアラート。CI環境（GitHub Actions）のCPU性能はばらつきがあるため、絶対値ではなく相対比較（KLGLOTT88比でLFモデルが何倍か）を指標とする

#### プロジェクト全体の設計思想の総括

本プロジェクトは「段階的に動くものを作り、各段階で品質を確認しながら進む」アプローチを採用した。これは source-filter model という物理モデルベースの音声合成において正しい判断であった。理由:

1. **物理モデルの検証可能性**: 各 Phase で「音が出る」「母音が聞こえる」という聴覚的な検証が可能
2. **問題の局所化**: Phase N で問題が発生した場合、Phase N-1 までは正常動作が保証されているため、原因の特定が容易
3. **技術リスクの段階的解消**: 最もリスクの高い Kelly-Lochbaum アルゴリズム（Phase 1）とLFモデル（Phase 5）を最初と最後に配置し、中間 Phase で安定基盤を構築
4. **ユーザー価値の早期提供**: Phase 2 の時点で「母音を鳴らせる楽器」として最小限の価値がある

一方、全体設計を最初に行う場合は、インターフェースの安定性と変更コストのバランスが課題となる。Vanilla TypeScript（フレームワークなし）の選択は以下のトレードオフがあった:

- **利点**: 依存パッケージゼロによるメンテナンスコストの極小化、AudioWorklet との親和性（フレームワークのランタイムが不要）、バンドルサイズの最小化、Web Audio API を直接操作する透明性
- **欠点**: UIコンポーネントの再利用性が低い（Phase 4-5 でスライダー/ボタンのパターンが重複）、DOMの手動管理が煩雑化する、状態管理を自前で設計する必要がある
- **総合判断**: 本プロジェクトのUI規模（スライダー数個 + Canvas 2枚 + ボタン群）ではフレームワーク導入のオーバーヘッドが利点を上回るため、Vanilla TypeScript が妥当。UIが大幅に複雑化する拡張（多数のプリセット管理画面、設定パネル等）を行う場合に再検討する

### 6.2 補足: レビューによる追加事項

#### 保守・運用の観点

Phase 5 はプロジェクトの最終フェーズであり、完了後の保守性を考慮した設計が重要:

- **依存パッケージの更新**: 本プロジェクトのランタイム依存はゼロ（Vanilla TypeScript + Web Audio API）。開発依存は Vite + TypeScript + Vitest のみであり、更新頻度は低い。Vite のメジャーバージョンアップ時はビルド設定の互換性を確認する程度で済む
- **ブラウザ API の変更リスク**: Web Audio API（AudioWorklet 含む）は W3C 勧告候補であり、破壊的変更のリスクは低い。ただし AudioWorklet の `addModule()` のモジュール解決仕様はブラウザ間で微差があるため、Vite のビルド方式（URL コンストラクタパターン）が将来も正しく動作するか、主要ブラウザのリリースノートを定期確認する
- **長期的な音質改善**: LFモデルのパラメータ変換式（Rd → Tp/Te/Ta）は研究が進行中の領域。Degottex (2010) 以降の改良版変換式が提案された場合、`rdToLFParams()` 関数の差し替えだけで対応できる設計にしておく

#### アクセシビリティの考慮

本アプリは声道の視覚的・聴覚的操作が主目的であり、スクリーンリーダー完全対応は現実的ではないが、以下は対応すべき:

- **キーボード操作**: Canvas 上の制御点をキーボード（Tab でフォーカス移動、矢印キーで断面積調整）で操作可能にする。これは視覚障害対応だけでなく、マウス操作より精密な調整を求めるユーザーにも有益
- **ボタン/スライダーのラベル**: 全ての `<button>` と `<input type="range">` に適切な `aria-label` を付与する。Phase 1-4 で追加した UI 要素を Phase 5 のリファインで一括整備する
- **色のコントラスト比**: CSSカスタムプロパティによるテーマ管理（6.1 の UI コンポーネント設計パターンで提案）において、WCAG 2.1 AA レベルのコントラスト比（4.5:1）を満たす配色を選択する

#### GitHub Pages デプロイの CI/CD パイプライン

セクション 7.2 で公開・デプロイの残タスクを列挙しているが、具体的な GitHub Actions ワークフローの構成を記載する:

```yaml
# .github/workflows/deploy.yml の構成概要
# トリガー: main ブランチへの push
# ステップ:
#   1. actions/checkout
#   2. actions/setup-node (Node.js LTS)
#   3. npm ci
#   4. npm run build (vite build, base設定でリポジトリ名を指定)
#   5. npm run test (Vitest によるユニットテスト)
#   6. actions/upload-pages-artifact (dist/ ディレクトリ)
#   7. actions/deploy-pages (GitHub Pages へデプロイ)
```

- テストが失敗した場合はデプロイを中断する（CI/CD ゲート）
- `vite.config.ts` の `base` は環境変数 `GITHUB_REPOSITORY` から動的に設定可能にしておくと、フォーク先でも設定変更なしでデプロイできる

---

## 7. 後続タスクへの連絡事項

### 7.1 将来拡張への準備

Phase 5 完了時点で以下の拡張ポイントを残しておく:

#### TTS対応への道筋
- 音素列 → 声道断面積シーケンスのマッピングテーブルを追加可能な設計にする
- 現在の母音プリセット（5母音）を拡張して子音の声道形状も追加可能にする
- 音素間の遷移時間テーブル（coarticulation rules）のデータ構造を検討しておく
- `vowel-presets.ts` を `phoneme-presets.ts` に将来リネームできるよう、インポート元を限定する

#### MIDI / OSC 対応への道筋
- F0 を AudioParam (k-rate) で管理しているため、MIDI CC → AudioParam の接続は容易
- Rd パラメータも AudioParam 化すれば、外部コントローラーからの制御が可能
- Web MIDI API の接続層を追加する場合、`src/midi/` ディレクトリを新設
- OSC は WebSocket 経由の受信が必要（別途サーバーが必要になるため、GitHub Pages デプロイとの整合性に注意）

#### 歌唱技法の高度化
- ビブラートの Rd 連動（ビブラート中に Rd も微小変動させると自然さが向上）
- フレーズのダイナミクスと Rd の連動（強い発声 → 低Rd、弱い発声 → 高Rd）
- これらのパラメータリンクを Auto Singer モジュールで制御可能にする拡張点を残す

### 7.2 公開・デプロイに向けた残タスク

Phase 5 完了後、GitHub Pages への公開に向けて以下を確認:

- [ ] `vite.config.ts` の `base` 設定がリポジトリ名と一致していること
- [ ] ビルド成果物（`dist/`）にすべてのアセットが含まれていること
- [ ] GitHub Actions ワークフローの設定（`.github/workflows/deploy.yml`）
- [ ] HTTPS 配信の確認（AudioWorklet の Secure Context 要件）
- [ ] README.md のスクリーンショット・使い方の記載
- [ ] ライセンスファイルの追加
- [ ] OGP メタタグの設定（SNS共有時のプレビュー）

### 7.3 パフォーマンスベースラインの記録

Phase 5 完了時に以下のベースライン値を記録し、将来の拡張時のリグレッション検出に使用する:

| 計測項目 | 目標値 | 記録欄 |
|---------|--------|--------|
| process() 実行時間（KLGLOTT88） | < 1.0ms | ___ms |
| process() 実行時間（LF モデル） | < 1.5ms | ___ms |
| process() 実行時間（LF + Aspiration） | < 1.8ms | ___ms |
| メインスレッド フレームレート | >= 58fps | ___fps |
<!-- REVIEW: 技術的正確性レビューにて修正 — 43x43行列 -->
| フォルマント計算時間（QR法 43x43） | < 3ms | ___ms |
| メモリ使用量（安定状態） | < 50MB | ___MB |
| 初回ロード時間（ビルド後） | < 2s | ___s |
| AudioWorklet 初期化時間 | < 500ms | ___ms |

計測環境の記録:
- ブラウザ / バージョン: ___
- OS: ___
- CPU: ___
- メモリ: ___

---

## 参考文献

- Liljencrants, J. & Fant, G. (1985). "A four-parameter model of glottal flow". STL-QPSR, 26(4), 1-13.
- Fant, G. (1995). "The LF-model revisited. Transformations and frequency domain analysis". STL-QPSR, 36(2-3), 119-156.
- Fant, G., Liljencrants, J. & Lin, Q. (1985). "A four-parameter model of glottal flow". STL-QPSR, 26(4), 1-13.
- Degottex, G., Roebel, A. & Rodet, X. (2010). "Pitch transposition and breathiness modification using a glottal source model and its adapted vocal-tract filter". ICASSP 2010.
- Klatt, D.H. & Klatt, L.C. (1990). "Analysis, synthesis, and perception of voice quality variations among female and male talkers". JASA, 87(2), 820-857.
